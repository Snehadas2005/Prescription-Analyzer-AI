[build]
builder = "nixpacks"

[deploy]
startCommand = "cd backend && source venv312/bin/activate && python main.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "on-failure"
restartPolicyMaxRetries = 10
